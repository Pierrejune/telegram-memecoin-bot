# Dépendances principales déjà présentes
pyTelegramBotAPI==4.14.0        # Pour le bot Telegram
flask==2.3.2                   # Serveur web pour Cloud Run
cachetools==5.3.1              # Cache pour optimisations (optionnel ici)
python-dotenv==1.0.0           # Gestion des variables d'environnement
web3==6.11.0                   # Interaction avec BSC
solders==0.21.0                # Bibliothèque Solana moderne
base58==2.1.1                  # Décodage des clés Solana
aiohttp==3.8.0                 # Requêtes HTTP asynchrones (pour Telethon ou autres)
telethon==1.36.0               # Client Telegram alternatif (non utilisé ici, mais conservé)
websockets==10.4               # WebSocket pour sniping Solana
solana==0.34.3                 # Bibliothèque Solana (utile pour certaines opérations)

# Ajouts nécessaires ou recommandés
requests==2.31.0               # Requêtes HTTP synchrones (utilisé dans le code)
asyncio==3.4.3                 # Gestion explicite d'asyncio (déjà inclus dans Python 3.7+, mais spécifié pour compatibilité)
statistics==1.0.3.5            # Pour la fonction mean dans monitor_and_sell
urllib3==1.26.18               # Dépendance pour requests (version stable pour éviter conflits)
werkzeug==2.3.7                # Dépendance Flask (version compatible avec Flask 2.3.2)
jinja2==3.1.2                  # Dépendance Flask pour templates (non utilisé ici, mais requis)
markupsafe==2.1.3              # Dépendance Jinja2
eth-account==0.11.0            # Gestion des signatures Ethereum/BSC (requis par web3)
hexbytes==0.3.1                # Gestion des hexadécimaux dans web3
protobuf==4.25.3               # Dépendance pour web3 (version stable)

# Optionnel : pour débogage ou amélioration
gunicorn==20.1.0               # Serveur WSGI recommandé pour Flask sur Cloud Run
